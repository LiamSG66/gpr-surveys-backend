# Reject Application

## Objective
Send a polite rejection email to the candidate after the admin marks their application as rejected.

## Steps
1. Fetch application and job posting → tools/fetch_application.py
2. Send rejection email to candidate → tools/send_email.py (template: application_rejected)
